#Convert a string to lowercase.
def lowercase(s):
    # Convert the string to lowercase, remove spaces
    s = s.lower()

    # Compare the string with its reversed version
    return s

print(lowercase('Racecar'))